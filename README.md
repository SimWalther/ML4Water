# ML4Water

## Requirements

Inside a Python environment, please install the requirements via `pip install -r requirements.txt`.

All the code necessary to process the data, train models, evaluation and model interperation is inside the utils folder and can be run inside the notebook `training_model_final.ipynb`.
